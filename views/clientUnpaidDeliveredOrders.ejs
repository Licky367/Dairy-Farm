<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Pending Payments</title>

<style>
body{
font-family:Arial;
background:#f4f6f8;
margin:0;
}

.header{
background:#0b5d1e;
color:#fff;
padding:20px;
text-align:center;
font-size:20px;
font-weight:bold;
}

.container{
padding:20px;
}

/* SUMMARY BOX */
.summary{
background:#fff;
padding:15px;
border-radius:10px;
margin-bottom:20px;
border:1px solid #eee;
}

.summary h3{
margin-bottom:10px;
}

.summary button{
width:auto;
padding:10px 16px;
}

/* TABLE */
table{
width:100%;
border-collapse:collapse;
background:#fff;
border-radius:10px;
overflow:hidden;
}

th, td{
padding:12px;
border-bottom:1px solid #eee;
text-align:left;
}

th{
background:#0b5d1e;
color:white;
}

button{
background:#0b5d1e;
color:white;
border:none;
padding:8px 12px;
border-radius:6px;
cursor:pointer;
}

.empty{
text-align:center;
padding:20px;
color:#777;
}
</style>
</head>

<body>

<div class="header">Delivered Orders Pending Payment</div>

<div class="container">

<% 
let totalArrears = 0;
if (orders && orders.length > 0) {
    orders.forEach(o => {
        totalArrears += Number(o.arrearAmount || 0);
    });
}
%>

<!-- ================= TOTAL ARREARS ================= -->
<% if (orders && orders.length > 0) { %>

<div class="summary">

<h3>Total Arrears: KES <%= totalArrears.toLocaleString() %></h3>

<a href="/payment-page?type=payArrears">
    <button>Pay All Arrears</button>
</a>

</div>

<% } %>

<!-- ================= TABLE ================= -->
<% if (orders && orders.length > 0) { %>

<table>
<thead>
<tr>
<th>Order ID</th>
<th>Date Delivered</th>
<th>Amount Due (KES)</th>
<th>Action</th>
</tr>
</thead>

<tbody>

<% orders.forEach(order => { %>

<tr>

<td><%= order._id %></td>

<td>
<%= order.deliveredAt 
    ? new Date(order.deliveredAt).toLocaleDateString() 
    : "N/A" %>
</td>

<td>
<%= Number(order.arrearAmount || 0).toLocaleString() %>
</td>

<td>
<a href="/payment-page?type=arrearAmount&orderId=<%= order._id %>">
<button>Pay Now</button>
</a>
</td>

</tr>

<% }) %>

</tbody>
</table>

<% } else { %>

<p class="empty">No pending payments found</p>

<% } %>

</div>

</body>
</html>